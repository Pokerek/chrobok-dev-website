export type SectionProps = {
  /** Anchor id — the scroll target for the sticky section nav (S-06), offset by `scroll-mt-anchor` to clear the sticky bar. */
  id: string;
  /** Extra classes for the outer <section>. */
  class?: string;
};
